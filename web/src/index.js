import React from 'react'; // importa o React dentro de todo arquivo JS que for utilizar HTML dentro dele
import ReactDOM from 'react-dom';
import App from './App';

ReactDOM.render(<App />, document.getElementById('root'));

// JSX (JavaScript + XML)